# ポートフォリオサイト リニューアル実施計画

作成日: 2026-07-08
対象: hamachan64.github.io（GitHub Pages / 静的サイト）

---

## 1. 目的

- サイトをモダンな構成・実装に刷新する
- 全体デザインを、作品群（**AR × カード × イマーシブ体験**）の世界観にマッチしたものに刷新する
- デザインに調和したアニメーション（スクロール連動・マイクロインタラクション等）を組み込む

> **要確認**: 参考として提示された X のポスト（@wurst_design）は認証制限により内容を確認できませんでした。
> 本計画では「モダンなポートフォリオサイトの定番モーション」（スムーススクロール、スクロール連動の
> 出現アニメーション、ホバー時の3Dチルト、ページ遷移演出など）を想定しています。
> 参考ポストの具体的な演出（例: 文字が分解される、画像がWebGLで歪む、横スクロール等）が分かれば、
> フェーズ5の内容を調整します。

---

## 2. 現状分析

### サイト構成
| ファイル | 内容 |
|---|---|
| `index.html` | トップ（Hero / About / Works ×9枠 / Contact） |
| `project01〜07.html` | 作品詳細 ×7（07は Coming soon） |
| `style.css` | 全スタイル（747行） |
| `script.js` | フェードイン用 IntersectionObserver 他 |

### 現状の課題
1. **JSエラー**: `script.js` が削除済みのカスタムカーソル要素 `cursorOutline` を参照しており、リンクホバー時に ReferenceError が発生する（`index.html` にはカーソル用の空 div も残存）
2. **重複コード**: 作品詳細ページ7枚がほぼ同一の `<style>` ブロックとヘッダー/フッターを各自コピーしており、修正が7箇所に波及する
3. **アセットが重い**: `assets/images/works/` が **合計 134MB**（特に 02 の MP4 が約90MB、01 が 28MB）。モバイル回線では実質閲覧不能なレベル
4. **フォント定義の不整合**: CSS は `M PLUS 2` / `Noto Sans JP` を参照しているが、実際に読み込んでいるのは `Orbitron` / `Zen Kaku Gothic New`
5. **デザインの方向性**: 現状は「サイバーパンク調ネオン（シアン×紫）」。作品の持つ「物理カードの手触り × 空間的な没入感」という質感とはややズレがある

---

## 3. デザインコンセプト（刷新方針）

### コンセプト: **"Spatial Deck" — カードから世界が立ち上がる**

作品群に共通するのは「**カードという物理メディアを入口に、空間・体験が展開する**」こと。
サイト自体をその体験のメタファーにする。

| 要素 | 方針 |
|---|---|
| **ベース** | ダークベースは継続しつつ、真っ黒(#050505)から深い空間色（例: `#0a0a12` 系の紺〜黒）へ。ネオンのギラつきを抑え、**ホログラフィック（玉虫色）グラデーション**をアクセントに |
| **モチーフ** | 「カード」を全面採用。Works はもちろん、About のスキルやセクション自体もカード/レイヤーとして扱い、奥行き（Z軸）を感じるレイアウト |
| **質感** | グラスモーフィズム（すりガラス）＋微細なノイズテクスチャで「AR越しに見た現実」感 |
| **タイポグラフィ** | 見出し: `Orbitron` は継続 or より現代的な `Space Grotesk` / `Unbounded` へ変更を検討。本文: `Zen Kaku Gothic New` 継続（日本語との相性が良い） |
| **余白** | セクション間の余白を大きく取り、1画面1メッセージの「見せる」構成へ |

### アニメーション方針（作品世界観との対応）

| 演出 | 内容 | 作品との関連 |
|---|---|---|
| スムーススクロール | Lenis による慣性スクロール | 空間を「移動する」感覚 |
| Hero 演出 | 名前のテキストが1文字ずつ立ち上がる＋背景にパーティクル/奥行きパララックス | AR召喚の「出現」 |
| Works カード | ホバーで **3Dチルト＋ホログラフィック反射**（マウス位置に追従する光沢） | トレカのホロカードそのもの |
| スクロール出現 | セクション/カードが奥から手前へ（translateZ/scale＋fade）段差をつけて出現 | カードを場に「展開」する |
| ページ遷移 | View Transitions API でカードが拡大して詳細ページになるシームレス遷移 | カードの中に「入る」 |
| マイクロ | ナビ・ボタンのホバー、スクロールプログレスバー、カスタムカーソル（復活・軽量版） | — |
| 配慮 | `prefers-reduced-motion` で全モーション無効化、60fps 維持（transform/opacity のみ使用） | — |

---

## 4. 技術スタック（提案）

| 項目 | 採用案 | 理由 |
|---|---|---|
| フレームワーク | **Astro** | 静的出力で GitHub Pages とそのまま相性◎。作品詳細をレイアウト共通化＋Markdown（Content Collections）で管理でき、課題2を根本解決。ビルド時の画像最適化も内蔵 |
| スタイリング | **CSSカスタムプロパティ＋素のCSS**（デザイントークン化） | 現行資産を活かせる。Tailwind は好みで選択可 |
| アニメーション | **GSAP（ScrollTrigger）＋ Lenis** | スクロール連動演出のデファクト。個人ポートフォリオは無償利用可 |
| 3D/装飾（任意） | Three.js は使わず、CSS 3D transform ＋ Canvas パーティクルで軽量に | 実装コストとロード速度のバランス |
| デプロイ | GitHub Actions → GitHub Pages | Astro 公式アクションあり。`main` push で自動デプロイ |

> **代替案**: フレームワークを入れず「現行の素の HTML/CSS/JS のまま構造を整理して GSAP を足す」ミニマム案も可能。
> ただし詳細ページ7枚の重複問題が残るため、長期運用（作品追加）を考えると Astro 化を推奨。

---

## 5. 実施フェーズ

### フェーズ0: 準備・資産整理（0.5日）
- [ ] 現行サイトを `legacy` ブランチ or タグで保全
- [ ] **アセット最適化**（最優先・効果大）
  - 画像: WebP/AVIF 変換＋リサイズ（表示幅の2倍まで）→ 目標: 各画像 200KB 以下
  - 動画: `02-01.MP4`(約90MB) を H.264/720p 数MB程度に再エンコード、`preload="none"`＋ポスター画像。もしくは YouTube 埋め込みへ
- [ ] `.DS_Store` を削除し `.gitignore` に追加

### フェーズ1: 基盤構築（0.5〜1日）
- [ ] Astro プロジェクト初期化（`src/pages`, `src/layouts`, `src/components`, `src/content/works/`）
- [ ] GitHub Actions デプロイワークフロー作成、Pages 設定を Actions に切替
- [ ] 既存URL維持: `project01.html` 等のパスをそのまま出力（外部リンク・名刺QRからの流入を壊さない）

### フェーズ2: デザインシステム（1日）
- [ ] デザイントークン定義（色 / タイポスケール / 余白 / 角丸 / シャドウ / z-depth / イージング・デュレーション）
- [ ] 共通コンポーネント: Header（スクロールで縮小）/ Footer / SectionTitle / HoloCard / Button
- [ ] フォント確定と `font-display: swap`、サブセット化

### フェーズ3: トップページ実装（1〜2日）
- [ ] Hero: 新デザイン（テキスト出現演出＋背景演出＋スクロールインジケータ）
- [ ] About: カード型レイアウト、スキルをタグ/カードで表現
- [ ] Works: HoloCard グリッド（3Dチルト＋ホロ反射）。Coming soon 枠は控えめなプレースホルダーに
- [ ] Contact: メール導線＋SNSリンクをアイコン化

### フェーズ4: 作品詳細のテンプレート化（1〜2日）
- [ ] 詳細ページ共通レイアウト（Hero画像 / メタ情報 / 概要 / メディアギャラリー / 前後の作品ナビ）を1つ作成
- [ ] 7作品分のコンテンツを Markdown（frontmatter: タイトル・役割・技術・年・サムネ）へ移行
- [ ] 動画・ギャラリーコンポーネント（Lightbox は任意）

### フェーズ5: アニメーション実装（1〜2日）
- [ ] Lenis スムーススクロール＋アンカーリンク対応
- [ ] GSAP ScrollTrigger: セクション出現 / カードのスタッガー展開 / Hero パララックス
- [ ] HoloCard: マウス追従チルト＋光沢シェーダー風グラデーション（CSS）
- [ ] View Transitions API によるトップ⇄詳細のカード拡大遷移（非対応ブラウザは通常遷移にフォールバック）
- [ ] `prefers-reduced-motion` 対応

### フェーズ6: 仕上げ・品質（1日）
- [ ] レスポンシブ最終調整（モバイルではチルト→タップ時の軽い演出に差し替え）
- [ ] Lighthouse 計測 → 目標: Performance 90+ / Accessibility 95+
- [ ] SEO/OGP: 各ページの title・description・OGP画像、`sitemap.xml`
- [ ] クロスブラウザ確認（Chrome / Safari / iOS Safari）
- [ ] スクリプトエラーゼロ確認（現行の cursorOutline バグ相当の残骸排除）

### フェーズ7: 公開（0.5日）
- [ ] `main` へマージ → 自動デプロイ
- [ ] 実機（スマホ）確認、旧URLからのアクセス確認

**想定合計: 6〜9日**（1日 = 集中作業ベース。空き時間での作業なら2〜3週間目安）

---

## 6. リスクと対策

| リスク | 対策 |
|---|---|
| 参考ポストの演出イメージと乖離 | フェーズ3着手前に Hero＋カード1枚の**モックを先行作成してすり合わせ** |
| 動画再エンコードで画質劣化 | 720p/H.264 CRF23 前後で試作し確認。ダメなら YouTube 限定公開埋め込み |
| View Transitions の未対応ブラウザ | 機能検出でフォールバック（通常遷移＋フェード） |
| GitHub Pages の URL 構造変化 | Astro の出力設定で `projectNN.html` 形式を維持 |
| アニメーション過多による酔い・重さ | reduced-motion 対応＋「1ビューにつき主役の動きは1つ」を原則に |

---

## 7. 決定事項（2026-07-08 確定）

1. **技術スタック**: Astro 化 ＋ GSAP(ScrollTrigger) ＋ Lenis
2. **参考演出**: https://oneshot-sakura.vercel.app/ のスクロール連動演出
   - 分析結果: GSAP + ScrollTrigger + Lenis 構成。ローディング演出 → Hero（文字を1字ずつ分解してスタッガー出現）、
     ピン留め＋スクラブ（`scrub: 0.4`）のストーリーテリングセクション、花びらの Canvas パーティクル、
     ベール（veil）が剥がれる画像リビール、番号付きパネルの順次出現
   - 本サイトへの翻訳: 花びら → ホログラフィックな光の粒子、ベール → ホロ光沢のマスクリビール、
     文字分解スタッガー・ピン留めスクラブは同様の手法で実装
3. **デザイントーン**: ホログラフィック寄りに刷新（ネオンのギラつきを抑え、玉虫色グラデーション＋グラス質感）
4. **見出しフォント**: **Space Grotesk** に変更（幾何学的でモダン、ホログラフィックとの相性◎）。本文は Zen Kaku Gothic New 継続
5. **動画の扱い**: YouTube 埋め込み（※ 幸せテープ便の動画 `02-01.MP4` は YouTube アップロード後に ID 差し替えが必要）
